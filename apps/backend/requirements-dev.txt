# Dependências de desenvolvimento/teste — não entram na imagem de produção.
# Instalar com: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
ruff>=0.1.0
